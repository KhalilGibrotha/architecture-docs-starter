# logo

Place a default repository logo at `assets/logo/logo.png`.

If you do not want a shared default logo, leave this folder empty and provide a
per-document logo override in your render manifest.

